-- ALTINAHUBV2
-- Studio-safe UI framework for your own Roblox game

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")

local player = Players.LocalPlayer

--// GUI
local gui = Instance.new("ScreenGui")
gui.Name = "ALTINAHUBV2"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

--// Main
local main = Instance.new("Frame")
main.Size = UDim2.fromOffset(570, 420)
main.Position = UDim2.new(.5, -285, .5, -210)
main.BackgroundColor3 = Color3.fromRGB(20,20,25)
main.BorderSizePixel = 0
main.Parent = gui

Instance.new("UICorner", main).CornerRadius = UDim.new(0,12)

--// Top bar
local top = Instance.new("Frame")
top.Size = UDim2.new(1,0,0,48)
top.BackgroundColor3 = Color3.fromRGB(28,28,35)
top.BorderSizePixel = 0
top.Parent = main

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1,-110,1,0)
title.Position = UDim2.fromOffset(15,0)
title.BackgroundTransparency = 1
title.Text = "ALTINAHUBV2"
title.TextColor3 = Color3.new(1,1,1)
title.TextSize = 20
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = top

-- Close
local close = Instance.new("TextButton")
close.Size = UDim2.fromOffset(40,32)
close.Position = UDim2.new(1,-48,0,8)
close.Text = "×"
close.TextSize = 24
close.TextColor3 = Color3.new(1,1,1)
close.BackgroundColor3 = Color3.fromRGB(180,50,60)
close.Parent = top
Instance.new("UICorner",close).CornerRadius = UDim.new(0,8)

--// Sidebar
local side = Instance.new("Frame")
side.Size = UDim2.fromOffset(145,372)
side.Position = UDim2.fromOffset(0,48)
side.BackgroundColor3 = Color3.fromRGB(24,24,30)
side.BorderSizePixel = 0
side.Parent = main

local content = Instance.new("ScrollingFrame")
content.Size = UDim2.new(1,-155,1,-58)
content.Position = UDim2.fromOffset(150,53)
content.BackgroundTransparency = 1
content.BorderSizePixel = 0
content.ScrollBarThickness = 4
content.Parent = main

local layout = Instance.new("UIListLayout")
layout.Padding = UDim.new(0,8)
layout.Parent = content

--// Floating toggle
local float = Instance.new("TextButton")
float.Size = UDim2.fromOffset(58,58)
float.Position = UDim2.new(0,20,.5,0)
float.Text = "A"
float.TextSize = 22
float.Font = Enum.Font.GothamBold
float.TextColor3 = Color3.new(1,1,1)
float.BackgroundColor3 = Color3.fromRGB(90,60,190)
float.Visible = false
float.Parent = gui
Instance.new("UICorner",float).CornerRadius = UDim.new(1,0)

--// Dragging
local dragging = false
local dragStart
local startPos

top.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
	or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = main.Position
	end
end)

UIS.InputChanged:Connect(function(input)
	if dragging and
		(input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch) then

		local delta = input.Position - dragStart

		main.Position = UDim2.new(
			startPos.X.Scale,
			startPos.X.Offset + delta.X,
			startPos.Y.Scale,
			startPos.Y.Offset + delta.Y
		)
	end
end)

UIS.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
	or input.UserInputType == Enum.UserInputType.Touch then
		dragging = false
	end
end)

--// Helpers
local function clear()
	for _,v in ipairs(content:GetChildren()) do
		if not v:IsA("UIListLayout") then
			v:Destroy()
		end
	end
end

local function button(text,callback)
	local b = Instance.new("TextButton")
	b.Size = UDim2.new(1,-10,0,38)
	b.BackgroundColor3 = Color3.fromRGB(35,35,44)
	b.TextColor3 = Color3.new(1,1,1)
	b.Text = text
	b.TextSize = 14
	b.Font = Enum.Font.Gotham
	b.Parent = content

	Instance.new("UICorner",b).CornerRadius = UDim.new(0,7)

	b.MouseButton1Click:Connect(callback)
	return b
end

local function toggle(text,callback)
	local state = false

	local b = button(text.."  [OFF]",function()
		state = not state
		b.Text = text.."  ["..(state and "ON" or "OFF").."]"
		callback(state)
	end)

	return b
end

local function dropdown(text,items,callback)
	local current = 1

	local b = button(text..": "..items[current],function()
		current += 1
		if current > #items then
			current = 1
		end

		b.Text = text..": "..items[current]
		callback(items[current])
	end)

	return b
end

local function header(text)
	local h = Instance.new("TextLabel")
	h.Size = UDim2.new(1,-10,0,30)
	h.BackgroundTransparency = 1
	h.Text = text
	h.TextColor3 = Color3.fromRGB(180,150,255)
	h.TextSize = 16
	h.Font = Enum.Font.GothamBold
	h.TextXAlignment = Enum.TextXAlignment.Left
	h.Parent = content
end

--// Game callback
local function gameAction(action,value)
	-- Connect your own game's systems here.
	print("[ALTINAHUBV2]",action,value)
end

--// Tabs
local tabs = {}

local function tab(name)
	local b = Instance.new("TextButton")
	b.Size = UDim2.new(1,-16,0,40)
	b.Position = UDim2.fromOffset(8,0)
	b.BackgroundColor3 = Color3.fromRGB(34,34,42)
	b.Text = name
	b.TextColor3 = Color3.new(1,1,1)
	b.TextSize = 14
	b.Font = Enum.Font.GothamBold
	b.Parent = side

	Instance.new("UICorner",b).CornerRadius = UDim.new(0,8)

	table.insert(tabs,b)
	return b
end

local eggTab = tab("🥚  EGGS")
eggTab.Position = UDim2.fromOffset(8,12)

local petTab = tab("🐾  PETS")
petTab.Position = UDim2.fromOffset(8,58)

local eventTab = tab("🎉  EVENTS")
eventTab.Position = UDim2.fromOffset(8,104)

local protectTab = tab("🛡️  PROTECTION")
protectTab.Position = UDim2.fromOffset(8,150)

local moveTab = tab("🏃  MOVEMENT")
moveTab.Position = UDim2.fromOffset(8,196)

local plotTab = tab("📦  MY PLOT")
plotTab.Position = UDim2.fromOffset(8,242)

local settingsTab = tab("⚙️  SETTINGS")
settingsTab.Position = UDim2.fromOffset(8,288)

--// EGGS
local function eggs()
	clear()

	header("🥚 EGG CONTROL")

	toggle("Auto Steal Eggs",function(v)
		gameAction("AutoStealEggs",v)
	end)

	dropdown("Area",{
		"Forest","Lake","Desert","Jungle","Snow",
		"Volcano","Abyss Ocean","Prehistoric","Cosmic",
		"Cherry","Titan Temple","Angels & Devils"
	},function(v)
		gameAction("Area",v)
	end)

	dropdown("Rarity",{
		"Common","Uncommon","Rare","Legendary",
		"Mythic","Secret","Internal","Divine"
	},function(v)
		gameAction("Rarity",v)
	end)

	dropdown("Mutation",{
		"Rainbow","Gold","Silver"
	},function(v)
		gameAction("Mutation",v)
	end)

	dropdown("Priority",{
		"Rarest","Weight"
	},function(v)
		gameAction("Priority",v)
	end)
end

--// PETS
local function pets()
	clear()

	header("🐾 PETS")

	toggle("Auto Equip Best",function(v)
		gameAction("AutoEquipBest",v)
	end)

	dropdown("Pet Rarity",{
		"Common","Uncommon","Rare","Legendary",
		"Mythic","Secret","Internal","Divine"
	},function(v)
		gameAction("PetRarity",v)
	end)
end

--// EVENTS
local function events()
	clear()

	header("🎉 EVENTS")

	toggle("Auto Event Rewards",function(v)
		gameAction("AutoEventRewards",v)
	end)

	toggle("Event Notifications",function(v)
		gameAction("EventNotifications",v)
	end)
end

--// PROTECTION
local function protection()
	clear()

	header("🛡️ PROTECTION")

	toggle("Anti Drop",function(v)
		gameAction("AntiDrop",v)
	end)

	toggle("Anti Fling",function(v)
		gameAction("AntiFling",v)
	end)

	toggle("Anti Trap",function(v)
		gameAction("AntiTrap",v)
	end)
end

--// MOVEMENT
local function movement()
	clear()

	header("🏃 MOVEMENT")

	dropdown("Movement Mode",{
		"Wall To Wall",
		"Straight"
	},function(v)
		gameAction("MovementMode",v)
	end)

	local speed = button("Speed: 16",function()
		gameAction("Speed",16)
	end)

	header("Tap Speed to cycle")

	local speeds = {16,50,100,250,500,750,1000}
	local index = 1

	speed.MouseButton1Click:Connect(function()
		index += 1
		if index > #speeds then
			index = 1
		end

		speed.Text = "Speed: "..speeds[index]
		gameAction("Speed",speeds[index])
	end)
end

--// PLOT
local function plot()
	clear()

	header("📦 MY PLOT EGGS")

	toggle("Auto Place Eggs",function(v)
		gameAction("AutoPlaceEggs",v)
	end)

	dropdown("Place Rarity",{
		"Common","Uncommon","Rare","Legendary",
		"Mythic","Secret","Internal","Divine"
	},function(v)
		gameAction("PlaceRarity",v)
	end)

	dropdown("Place Mutation",{
		"Rainbow","Gold","Silver"
	},function(v)
		gameAction("PlaceMutation",v)
	end)

	toggle("Auto Hatch Ready Eggs",function(v)
		gameAction("AutoHatchReadyEggs",v)
	end)

	dropdown("Hatch Rarity",{
		"Common","Uncommon","Rare","Legendary",
		"Mythic","Secret","Internal","Divine"
	},function(v)
		gameAction("HatchRarity",v)
	end)
end

--// SETTINGS
local function settings()
	clear()

	header("⚙️ SETTINGS")

	toggle("UI Sounds",function(v)
		gameAction("UISounds",v)
	end)

	toggle("Notifications",function(v)
		gameAction("Notifications",v)
	end)

	toggle("Compact Mode",function(v)
		gameAction("CompactMode",v)
	end)
end

eggTab.MouseButton1Click = eggs
petTab.MouseButton1Click = pets
eventTab.MouseButton1Click = events
protectTab.MouseButton1Click = protection
moveTab.MouseButton1Click = movement
plotTab.MouseButton1Click = plot
settingsTab.MouseButton1Click = settings

--// Close / Open
close.MouseButton1Click:Connect(function()
	main.Visible = false
	float.Visible = true
end)

float.MouseButton1Click:Connect(function()
	main.Visible = true
	float.Visible = false
end)

--// Initial tab
eggs()
